import { Noise2D } from './InteractiveComponents';

export const metadata = {
  demo: Noise2D,
  title: 'Noise (2D)'
};

If we use a 2D noise function, we can start to create some interesting effects that are almost like hills and valleys in a terrain.