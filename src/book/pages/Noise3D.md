import { Noise3D } from './InteractiveComponents';

export const metadata = {
  demo: Noise3D,
  title: 'Noise (3D)'
};

The nice thing about noise is that you can add an extra dimension to create smooth perturbations.

Try changing the `Time` slider here: